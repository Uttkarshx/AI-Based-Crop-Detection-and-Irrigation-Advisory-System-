study_area:
  name: "rajasthan"
  display_name: "Rajasthan"

  country: "India"

  enabled: true

  coordinate_system:
    crs: "EPSG:4326"

  bounds:
    min_latitude: 23.0
    max_latitude: 30.2
    min_longitude: 69.5
    max_longitude: 78.3

  crops:
    - "wheat"
    - "rice"
    - "maize"

  datasets:
    sentinel1:
      enabled: true
      path: "data/raw/sentinel1"
    sentinel2:
      enabled: true
      path: "data/raw/sentinel2"
      temporal: true
    soilgrids:
      enabled: true
      path: "data/raw/soil"
    nasa_power:
      enabled: true
      path: "data/raw/meteorological/nasa_power"
    ground_truth:
      enabled: true
      train_labels_path: "data/raw/ground_truth/train_labels"
      test_labels_path: "data/raw/ground_truth/test_labels"
